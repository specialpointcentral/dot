# ~/.secrets.zsh — API keys and tokens
# Copy this file to ~/.secrets.zsh and fill in your real values.
# NEVER commit ~/.secrets.zsh to version control.

export ANTHROPIC_BASE_URL="https://your-api-endpoint/api"
export ANTHROPIC_AUTH_TOKEN="your-token-here"
